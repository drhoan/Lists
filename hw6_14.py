# Write a function called is_sorted(lst) that takes a list as a parameter 
# and returns True if the list is sorted in ascending order and False otherwise. For example:
# is_sorted([1, 2, 2]) should return True
# is_sorted(['b', 'a']) should return False
# Hint: use the built-in sorted function that returns a new sorted list and compare it to the original list.

def is_sorted(lst):
    # Replace the pass statement with your code
    pass

if __name__ == "__main__":
    print(is_sorted([1, 2, 2]))  # True
    print(is_sorted(['b', 'a']))  # False